# Production signaling endpoint (Cloudflare Worker). Public value — safe to commit.
# `npm run dev` ignores this and uses the local Node signaling server (ws://localhost:8787).
VITE_SIGNALING_URL=wss://streamlined-signaling.datrayez911.workers.dev
